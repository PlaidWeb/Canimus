# Format extensions

Optional extensions to the format are defined here. The following extensions currently exist:

* [Pagination](pagination.md): The mechanism by which large collections may be split up into multiple documents
